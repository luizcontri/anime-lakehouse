apiVersion: v1
kind: Service
metadata:
  name: airflow-db
  namespace: airflow
  labels:
    app.kubernetes.io/name: airflow-db
    app.kubernetes.io/part-of: anime-lakehouse
spec:
  selector:
    app.kubernetes.io/name: airflow-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
