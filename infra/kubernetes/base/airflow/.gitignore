# Plain-text secret files with real credentials — never commit these
*-secret.yaml
*-credentials.yaml
# Exception: sealed secrets are safe to commit (encrypted)
!airflow-sealed-secret.yaml
!airflow-git-sealed-secret.yaml
